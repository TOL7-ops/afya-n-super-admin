# MISSING_ENDPOINTS.md — Afya Super Admin

> Complete audit of every gap between the UI and the backend API.
> Generated by scanning all components, hooks, services, modals, and constants.
> Last updated: June 2026

---

## How to Read This File

| Symbol | Meaning |
|--------|---------|
| 🔴 | **Backend endpoint missing** — needs to be built |
| 🟡 | **Backend schema gap** — endpoint exists but field is missing from response |
| 🟠 | **Frontend gap** — endpoint exists, data available, frontend not wired |
| ✅ | **Integrated** — live API call, real data |

---

## Table of Contents

1. [Institution Management](#1-institution-management)
2. [License Management](#2-license-management)
3. [Revenue](#3-revenue)
4. [Users](#4-users)
5. [Analytics](#5-analytics)
6. [Audit Log](#6-audit-log)
7. [Dashboard](#7-dashboard)
8. [Settings](#8-settings)
9. [Modals](#9-modals)
10. [Missing Response Fields Summary](#10-missing-response-fields-summary)
11. [Missing Endpoints Summary](#11-missing-endpoints-summary)
12. [Priority Order for Backend Team](#12-priority-order-for-backend-team)

---

## 1. Institution Management

### 1.1 🔴 Edit Institution
| | |
|---|---|
| **UI Action** | "Edit" button in Institutions table |
| **Current behaviour** | Toast: `"Edit {name} — backend endpoint pending"` |
| **Modal** | `EditInstitutionModal.tsx` is fully built but never mounted |
| **Endpoint needed** | `PATCH /api/v1/facilities/{facility_id}` |
| **Request body needed** | `{ name, region, address, contact_number, license_plan }` |
| **Page** | `InstitutionsView.tsx` |

---

### 1.2 🔴 Reject Pending Institution
| | |
|---|---|
| **UI Action** | "✕ Reject" button on Dashboard pending approvals table |
| **Current behaviour** | Toast: `"Rejection sent"` — institution stays in list, no API call |
| **Endpoint needed** | `DELETE /api/v1/facilities/{facility_id}` or `PATCH /api/v1/facilities/{facility_id}/reject` |
| **Page** | `DashboardView.tsx` |

---

### 1.3 🟡 Institution Type Field Missing
| | |
|---|---|
| **UI Column** | "Type" (Government / NGO / Hospital / Pharmacy / Employer) |
| **Current behaviour** | All institutions display as "NGO" (hardcoded fallback) |
| **API gap** | `FacilityResponse` does not include `type` field |
| **Fix needed** | Add `type: string` to `GET /api/v1/facilities/` response |
| **Affected pages** | `InstitutionsView`, `DashboardView`, `RevenueView`, pending approvals |

---

### 1.4 🟡 Field Workers & Total Screened Missing from Facilities
| | |
|---|---|
| **UI Columns** | "Field Workers", "Total Screened" (Dashboard top institutions table) |
| **Current behaviour** | Both columns always show `0` |
| **API gap** | `FacilityResponse` has no `field_worker_count` or `total_screened` |
| **Fix needed** | Add per-facility counts to `FacilityResponse` OR supply via a per-facility analytics endpoint |
| **Affected pages** | `DashboardView.tsx` top performers table |

---

## 2. License Management

### 2.1 🔴 Renew License
| | |
|---|---|
| **UI Action** | "Renew" button in License Registry |
| **Current behaviour** | Toast: `"Renewal noted for {name} — backend endpoint pending"` |
| **Endpoint needed** | `POST /api/v1/facilities/{facility_id}/renew` |
| **Suggested body** | `{ new_license_plan: string, new_expiry_date: string }` |
| **Page** | `LicensesView.tsx` |

---

### 2.2 🔴 Send Renewal Reminder Email
| | |
|---|---|
| **UI Action** | "Remind" button for Expiring licenses |
| **Current behaviour** | Toast: `"Reminder queued for {name} — backend endpoint pending"` |
| **Endpoint needed** | `POST /api/v1/facilities/{facility_id}/send-renewal-email` |
| **Page** | `LicensesView.tsx` |

---

### 2.3 🔴 Convert Trial to Paid Plan
| | |
|---|---|
| **UI Action** | "Convert to Paid" button → `ConvertTrialModal` → user selects plan → clicks confirm |
| **Current behaviour** | Calls `PATCH /suspend?active=true` (wrong) — plan selection is silently discarded |
| **Endpoint needed** | `PATCH /api/v1/facilities/{facility_id}` accepting `{ license_plan: string }` |
| **Page** | `ConvertTrialModal.tsx`, `LicensesView.tsx` |

---

### 2.4 🟠 License View Modal Never Opens
| | |
|---|---|
| **UI Action** | "View" button next to Active/Expiring licenses |
| **Current behaviour** | Toast: `"{name} license details"` — modal never opens |
| **Root cause** | `LicenseViewModal.tsx` is fully built but never imported in `AdminShell.tsx` |
| **Fix** | Frontend-only — import and mount modal, wire to "View" button, data already available |
| **Page** | `LicensesView.tsx` |

---

### 2.5 🟡 Seat Utilisation Missing
| | |
|---|---|
| **UI KPI** | "Seat Utilisation" card — shows `—` |
| **API gap** | `FacilityResponse` has no `seats` or `seats_used` field |
| **Fix needed** | Add `seats: number` and `seats_used: number` to `FacilityResponse` |
| **Page** | `LicensesView.tsx` |

---

### 2.6 🟡 License Start Date / Payment Method Missing
| | |
|---|---|
| **UI Columns** | "Start Date", "Amount (GHS)", "Payment Method" in original License Registry design |
| **Current behaviour** | Table omits these columns entirely; revenue table uses `created_at` as wrong proxy for start date |
| **API gap** | `FacilityResponse` has no `license_starts_at`, `license_amount`, or `payment_method` |
| **Fix needed** | Add to `FacilityResponse` or create a `/licenses/` sub-resource |

---

## 3. Revenue

### 3.1 🔴 Monthly Revenue Breakdown (Entire Billing Resource Missing)
| | |
|---|---|
| **UI Cards** | "Revenue This Month", "Monthly Revenue — Last 6 Months" chart |
| **Current behaviour** | Month KPI shows `—`, chart shows flat grey placeholder bars with "billing API not yet available" |
| **Endpoints needed** | `GET /api/v1/billing/monthly-summary` and `GET /api/v1/billing/revenue-timeline` |
| **Page** | `RevenueView.tsx` |

---

### 3.2 🔴 Transaction History (Payments)
| | |
|---|---|
| **UI Table** | Transaction History — Institution, Type, Plan, Amount, Payment Date, Method, Period, Status |
| **Current behaviour** | Shows facility list as a proxy — no payment dates, methods, periods, or paid/due status |
| **Endpoints needed** | `GET /api/v1/billing/transactions` |
| **Page** | `RevenueView.tsx` |

---

### 3.3 🔴 Revenue Export
| | |
|---|---|
| **UI Action** | "Export Report ↓" button |
| **Current behaviour** | Toast: `"Revenue report exported"` — no file downloaded |
| **Endpoint needed** | `GET /api/v1/billing/export-csv` |
| **Page** | `RevenueView.tsx` |

---

### 3.4 🟡 Institution Type for Revenue Chart
| | |
|---|---|
| **UI Card** | "Revenue by Institution Type" — should show Government / Hospital / NGO / Pharmacy |
| **Current behaviour** | Shows Enterprise / Standard / Other (plan tier proxy) |
| **API gap** | `FacilityResponse` missing `type` field (same as §1.3) |

---

## 4. Users

### 4.1 🔴 Reactivate User
| | |
|---|---|
| **UI Action** | "Reactivate" button for suspended users |
| **Current behaviour** | Button is permanently `disabled` with tooltip `"Reactivate endpoint not yet available"` |
| **Endpoint needed** | `PATCH /api/v1/users/{user_id}/activate` |
| **Page** | `UsersView.tsx` |

---

### 4.2 🔴 Add New User
| | |
|---|---|
| **UI** | No "Add User" button exists — functionality entirely absent |
| **Endpoint** | `POST /api/v1/users/register` (exists in Swagger, not wired) |
| **What's needed** | Build "Add User" modal with name/email/role/facility fields, wire to endpoint |
| **Page** | `UsersView.tsx` |

---

### 4.3 🟡 Institution Name Not Resolved in Users Table
| | |
|---|---|
| **UI Column** | "Institution" — should show facility name |
| **Current behaviour** | Shows raw ID like `#1`, `#2` |
| **Root cause** | Frontend-only fix — facilities list is already loaded in `AdminShell`; just cross-reference `facility_id` |
| **No new endpoint needed** | Pass `facilities` prop into `UsersView` and do a lookup |
| **Page** | `UsersView.tsx` |

---

### 4.4 🟡 Last Login Field Missing
| | |
|---|---|
| **UI Column** | "Last Login" |
| **Current behaviour** | Column is absent from the live table (was removed when switching to real API) |
| **API gap** | `UserResponse` has no `last_login` field |
| **Fix needed** | Add `last_login: string | null` to `UserResponse` from `GET /api/v1/users/` |
| **Page** | `UsersView.tsx` |

---

## 5. Analytics

### 5.1 🔴 Regional Breakdown
| | |
|---|---|
| **UI Section** | "Hypertension by Region" — Volta Region, Greater Accra, Ashanti with screened/highBP bars |
| **Current behaviour** | Entire section removed (was using `REGION_STATS` mock) |
| **Endpoint needed** | `GET /api/v1/analytics/summary` should include `region_breakdown: Array<{region, screened, high_bp}>` |
| **Page** | `AnalyticsView.tsx` |

---

### 5.2 🔴 Age Distribution
| | |
|---|---|
| **UI Section** | "Age Distribution of High-BP Patients" — 18–30, 31–45, 46–60, 60+ |
| **Current behaviour** | Entire section removed (was using `AGE_DISTRIBUTION` mock) |
| **Endpoint needed** | `GET /api/v1/analytics/summary` should include `age_distribution: Record<string, number>` |
| **Page** | `AnalyticsView.tsx` |

---

### 5.3 🔴 Gender Breakdown
| | |
|---|---|
| **UI Section** | "BP Risk by Gender" — Female/Male with Normal/Elevated/High-Crisis breakdown |
| **Current behaviour** | Entire section removed (was using `GENDER_STATS` mock) |
| **Endpoint needed** | `GET /api/v1/analytics/summary` should include `gender_stats: {female: {...}, male: {...}}` |
| **Page** | `AnalyticsView.tsx` |

---

### 5.4 🔴 Monthly Risk Level Trend
| | |
|---|---|
| **UI Section** | "Risk Level Trend — Monthly (Last 6 Months)" stacked bar chart — Normal / Elevated / Stage 1/2 / Crisis by month |
| **Current behaviour** | Uses `RISK_TREND_DATA` static fallback from `constants/index.ts` (still showing mock data) |
| **Endpoint needed** | `GET /api/v1/analytics/timeline` needs a `category_breakdown` option — e.g. `?breakdown=category` returning per-category counts per period |
| **Page** | `AnalyticsView.tsx` → `RiskTrendChart.tsx` |

---

### 5.5 🔴 Detection Rate Trend
| | |
|---|---|
| **UI Section** | "Hypertension Detection Rate — Trend" monthly detection % bars |
| **Current behaviour** | Falls back to `DETECTION_RATE_TREND` static mock from `constants/index.ts` when `bpDist` is empty |
| **Endpoint needed** | `GET /api/v1/analytics/detection-rate-trend` or extended timeline response |
| **Page** | `AnalyticsView.tsx` |

---

### 5.6 🔴 Per-Facility Adherence Table
| | |
|---|---|
| **UI Section** | "Follow-up & Adherence Rates by Institution" table |
| **Current behaviour** | Removed (was using `ADHERENCE_ROWS` mock with 3 institutions) |
| **What's needed** | Loop `GET /api/v1/analytics/adherence?facility_id={id}` for each facility, or add a bulk endpoint |
| **Endpoint needed** | `GET /api/v1/analytics/adherence/by-facility` returning array of per-facility stats |
| **Page** | `AnalyticsView.tsx` |

---

### 5.7 🟠 Analytics Export Not Wired on Dashboard
| | |
|---|---|
| **UI Action** | "Export Report" button on Dashboard |
| **Current behaviour** | Toast: `"Report generating…"` — no file downloaded |
| **Root cause** | Frontend-only — `exportAnalyticsCsv()` service function already exists, just not called |
| **Fix** | Replace toast with `exportAnalyticsCsv()` call in `DashboardView.tsx` |

---

## 6. Audit Log

### 6.1 🔴 Dedicated Audit Export Endpoint
| | |
|---|---|
| **UI Action** | "Export Log ↓" button |
| **Current behaviour** | Downloads screening patient CSV from `GET /api/v1/analytics/export-csv` — wrong data |
| **Endpoint needed** | `GET /api/v1/users/activity-logs/export-csv` returning audit entries as CSV |
| **Page** | `AuditView.tsx` |

---

### 6.2 🟡 Agent Name Not Resolved in Log Entries
| | |
|---|---|
| **UI** | Audit entries should show human-readable agent name (e.g. "Dr. Esi Asante logged in") |
| **Current behaviour** | Shows `action + details` text — `agent_id` (integer) never resolved to a name |
| **API gap** | `AgentActivityLogResponse` has `agent_id: number` but no `agent_name: string` |
| **Fix needed** | Add `agent_name: string` to `AgentActivityLogResponse` |
| **Page** | `AuditView.tsx` |

---

## 7. Dashboard

### 7.1 🟠 Top Institutions Table Shows All Zeros
| | |
|---|---|
| **UI Columns** | Field Workers, Screened, On Treatment, Adherence |
| **Current behaviour** | All four numeric columns display `0` for every institution |
| **Root cause** | `AdminShell.tsx` hardcodes `fieldWorkers: 0, screened: 0, onTreatment: 0, adherence: 0` (TODO comment) |
| **API gap** | These are per-facility stats not returned by `FacilityResponse` |
| **Fix options** | (A) Add denormalized stats to `FacilityResponse`, or (B) call `GET /api/v1/analytics/summary?facility_id={id}` per facility |

---

### 7.2 🟡 Institution Type in Pending Approvals Always "NGO"
| | |
|---|---|
| **UI Column** | "Type" badge on pending approval rows |
| **Current behaviour** | Always shows "NGO" chip |
| **Root cause** | Same as §1.3 — `type` missing from `FacilityResponse` |

---

### 7.3 🟠 "Export Report" Button Not Wired
| | |
|---|---|
| **UI Action** | "Export Report" button top-right of Dashboard |
| **Current behaviour** | Toast: `"Report generating…"` — no file downloaded |
| **Fix** | Frontend-only — call existing `exportAnalyticsCsv()` from `services/analytics.service.ts` |

---

## 8. Settings

### 8.1 🔴 WhatsApp Configuration — All Values Hardcoded
| | |
|---|---|
| **UI Fields** | BSP name (`"360dialog (Active)"`), Business Number (`"+233 XX XXXX XXXX"`), Delivery Rate (`"99.1%"`) |
| **Current behaviour** | All three are static strings — never fetched from API |
| **Endpoint needed** | `GET /api/v1/settings/whatsapp` or extend `GET /api/v1/templates/` to include BSP metadata |
| **Configure BSP button** | Toast: `"BSP settings opened"` — no action |
| **Page** | `SettingsView.tsx` |

---

### 8.2 🔴 WhatsApp Message Templates Not Loaded
| | |
|---|---|
| **UI Section** | WhatsApp Configuration — should show/edit message templates |
| **Current behaviour** | Static display — no templates loaded from API |
| **Endpoint** | `GET /api/v1/templates/` (exists in Swagger, not wired) |
| **Update action** | `PUT /api/v1/templates/{template_id}` (exists in Swagger, not wired) |
| **Page** | `SettingsView.tsx` |

---

### 8.3 🔴 Compliance Report Generation
| | |
|---|---|
| **UI Action** | "Generate Compliance Report" button |
| **Current behaviour** | Toast: `"Compliance report generated"` — no file, no API call |
| **Endpoint needed** | `GET /api/v1/compliance/report` or `GET /api/v1/analytics/compliance-export` |
| **Page** | `SettingsView.tsx` |

---

### 8.4 🟠 BP Thresholds and Role Permissions Are Hardcoded
| | |
|---|---|
| **UI Sections** | "BP Classification Thresholds", "Role Permissions" |
| **Current behaviour** | Static display from hardcoded arrays — cannot be updated |
| **Note** | These may be intentionally static (WHO/AHA standard values) — confirm with backend team |
| **If live data needed** | `GET /api/v1/settings/bp-thresholds`, `GET /api/v1/settings/role-permissions` |

---

## 9. Modals

### 9.1 🟠 EditInstitutionModal — Built but Never Mounted
| | |
|---|---|
| **File** | `components/modals/EditInstitutionModal.tsx` |
| **Status** | Fully implemented — never imported in `AdminShell.tsx` |
| **What's blocking** | No `PATCH /api/v1/facilities/{id}` endpoint (see §1.1) |
| **Fix** | Once backend adds endpoint: import modal in `AdminShell`, add `editModal` state, wire to "Edit" button |

---

### 9.2 🟠 LicenseViewModal — Built but Never Mounted
| | |
|---|---|
| **File** | `components/modals/LicenseViewModal.tsx` |
| **Status** | Fully implemented — never imported in `AdminShell.tsx` |
| **What's blocking** | Frontend-only — data already available from facilities list |
| **Fix** | Import modal in `AdminShell`, wire to "View" button in `LicensesView` |

---

### 9.3 🟡 IssueLicenseModal Creates Wrong Resource
| | |
|---|---|
| **UI** | "Issue License" → select institution from list → choose plan → confirm |
| **Current behaviour** | Calls `POST /api/v1/facilities/` (creates a new facility) with a fabricated email |
| **Correct behaviour** | Should update `license_plan` on an existing facility |
| **Fix needed** | `PATCH /api/v1/facilities/{id}` + change modal to show institution dropdown instead of free-text |
| **Page** | `IssueLicenseModal.tsx` + `AdminShell.tsx` |

---

### 9.4 🟡 CreateInstitutionModal Silently Drops 4 Fields
| | |
|---|---|
| **Form fields collected** | Institution Type, Admin Phone, Field Worker Seats, Notes |
| **Current behaviour** | All four are collected by the form but not included in `FacilityCreateWithoutUser` payload |
| **Fix needed** | Add `type`, `contact_number`, `seats`, `notes` to `POST /api/v1/facilities/` request schema |
| **Page** | `CreateInstitutionModal.tsx` |

---

### 9.5 🟡 Onboarding Token Shows Placeholder Instead of Real Token
| | |
|---|---|
| **UI** | Email preview modal shows `AFYA-XXXX-XXXX-XXXX` |
| **Current behaviour** | Token is generated client-side before the API call — always placeholder |
| **Fix needed** | Add `setup_token: string` to `FacilityResponse` from `POST /api/v1/facilities/`; reverse flow so preview shows real token after creation |

---

## 10. Missing Response Fields Summary

Fields that need to be **added to existing API responses** (no new endpoints required — schema changes only):

| Field | Add To | Needed By |
|-------|--------|-----------|
| `type` | `FacilityResponse` | Institutions table, Dashboard, Revenue, Pending Approvals |
| `field_worker_count` | `FacilityResponse` | Dashboard top institutions |
| `total_screened` | `FacilityResponse` | Dashboard top institutions |
| `seats` | `FacilityResponse` | Licenses seat utilisation KPI |
| `seats_used` | `FacilityResponse` | Licenses seat utilisation KPI |
| `license_starts_at` | `FacilityResponse` | Revenue/License start date column |
| `license_amount` | `FacilityResponse` | Revenue/License amount column |
| `payment_method` | `FacilityResponse` | Revenue transaction table |
| `last_login` | `UserResponse` | Users table last login column |
| `agent_name` | `AgentActivityLogResponse` | Audit log human-readable entries |
| `setup_token` | `FacilityResponse` (POST) | Onboarding email real token |
| `region_breakdown` | `AnalyticsSummaryResponse` | Analytics hypertension by region |
| `age_distribution` | `AnalyticsSummaryResponse` | Analytics age distribution |
| `gender_stats` | `AnalyticsSummaryResponse` | Analytics gender breakdown |

---

## 11. Missing Endpoints Summary

Completely new endpoints the backend needs to build:

| Priority | Method | Endpoint | Blocks |
|----------|--------|----------|--------|
| 🔴 HIGH | `PATCH` | `/api/v1/facilities/{id}` | Edit institution, Convert trial, Issue license to existing facility |
| 🔴 HIGH | `PATCH` | `/api/v1/users/{id}/activate` | Reactivate suspended users |
| 🔴 HIGH | `DELETE/PATCH` | `/api/v1/facilities/{id}/reject` | Reject pending institution |
| 🔴 MEDIUM | `POST` | `/api/v1/facilities/{id}/renew` | Renew license button |
| 🔴 MEDIUM | `POST` | `/api/v1/facilities/{id}/send-renewal-email` | Renewal reminder button |
| 🔴 MEDIUM | `GET` | `/api/v1/billing/monthly-summary` | Revenue This Month KPI |
| 🔴 MEDIUM | `GET` | `/api/v1/billing/revenue-timeline` | Monthly Revenue chart |
| 🔴 MEDIUM | `GET` | `/api/v1/billing/transactions` | Transaction History table |
| 🔴 MEDIUM | `GET` | `/api/v1/billing/export-csv` | Revenue export button |
| 🔴 MEDIUM | `GET` | `/api/v1/users/activity-logs/export-csv` | Audit log export (currently exports wrong data) |
| 🔴 MEDIUM | `GET` | `/api/v1/analytics/adherence/by-facility` | Per-facility adherence table in Analytics |
| 🟡 LOW | `GET` | `/api/v1/settings/whatsapp` | WhatsApp BSP config in Settings |
| 🟡 LOW | `GET` | `/api/v1/compliance/report` | Generate Compliance Report button |
| 🔵 FUTURE | `GET` | `/api/v1/analytics/detection-rate-trend` | Detection rate trend chart |

---

## 12. Priority Order for Backend Team

### 🔴 Must-Have (Blocking UI features that are visibly broken)

| # | Task | Impact |
|---|------|--------|
| 1 | Add `type` field to `FacilityResponse` | Fixes "NGO" hardcode across 5+ places |
| 2 | `PATCH /api/v1/facilities/{id}` general update | Unblocks Edit, Convert Trial, Issue License to existing |
| 3 | `PATCH /api/v1/users/{id}/activate` | Fixes disabled Reactivate button |
| 4 | Add `last_login` to `UserResponse` | Restores missing column in Users table |
| 5 | Add `agent_name` to `AgentActivityLogResponse` | Makes audit entries human-readable |
| 6 | `PATCH/DELETE /api/v1/facilities/{id}/reject` | Makes Reject button functional |
| 7 | Add `setup_token` to `POST /api/v1/facilities/` response | Real token in onboarding email preview |

### 🟠 Should-Have (Significant data gaps visible to users)

| # | Task | Impact |
|---|------|--------|
| 8 | Add `field_worker_count`, `total_screened` to `FacilityResponse` | Dashboard top institutions all-zeros |
| 9 | Add `seats`, `seats_used` to `FacilityResponse` | License seat utilisation KPI |
| 10 | `GET /api/v1/billing/transactions` + monthly summary | Revenue page real data |
| 11 | `GET /api/v1/users/activity-logs/export-csv` | Audit export downloads correct data |
| 12 | `POST /api/v1/facilities/{id}/renew` | Renew license button works |
| 13 | Add `region_breakdown`, `age_distribution`, `gender_stats` to analytics summary | Restores 3 missing Analytics sections |

### 🔵 Nice-to-Have (Enhancement features)

| # | Task | Impact |
|---|------|--------|
| 14 | `GET /api/v1/analytics/adherence/by-facility` | Per-facility adherence table in Analytics |
| 15 | `POST /api/v1/facilities/{id}/send-renewal-email` | Renewal reminder button works |
| 16 | `GET/PUT /api/v1/templates/` wired in Settings | Live WhatsApp template editing |
| 17 | `GET /api/v1/settings/whatsapp` | Live BSP config display |
| 18 | `GET /api/v1/billing/export-csv` | Revenue export button works |
| 19 | Timeline category breakdown for Risk Trend chart | Restores risk level trend section |

---

*This file is auto-generated from a full codebase scan. Re-run the analysis after each sprint to track progress.*
*Backend: `https://afya-backend-production.up.railway.app` | Frontend: Next.js 16 App Router*
