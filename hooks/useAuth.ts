'use client';

/**
 * Convenience hook for consuming the AuthContext.
 * Provides login, logout, user, loading, and isAuthenticated.
 */
export { useAuthContext as useAuth } from '@/context/AuthContext';
